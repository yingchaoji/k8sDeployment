---

apiVersion: v1
kind: Service
metadata:
  name: demo-servicexxx
  namespace: jason
  labels:
    app: demo-servicexxx
spec:
  type: NodePort
  ports:
  - port: 8701
    targetPort: 8701
    protocol: TCP
    name: http
  selector:
    app: demo-pod
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-deploymentxxx
  namespace: jason
spec:
  selector:
    matchLabels:
      app: demo-pod
  replicas: 1
  template:
    metadata:
      labels:
        app: demo-pod
    spec:
      containers:
      - name:  demo-container
        image: localhost:5000/k8sdemo:1.0
        ports:
        - containerPort: 8701
